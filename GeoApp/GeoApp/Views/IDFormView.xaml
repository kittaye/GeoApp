<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GeoApp.IDFormView"
             xmlns:local="clr-namespace:GeoApp"
             Title = "Welcome to GeoApp"
             BackgroundColor="Default">

    <ContentPage.BindingContext>
        <local:IDFormViewModel x:Name="viewModel"/>
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <StackLayout>
            <Label Margin="20,0,20,0" Text="Please input an ID below. This will be your display name throughout the app."
                   TextColor="Black"
                   FontSize="Large"
                   VerticalOptions="CenterAndExpand" 
                   HorizontalOptions="CenterAndExpand"
                   />

            <Entry Text="{Binding IDEntry}" AutomationId="IDText" Margin="20,20,20,150" Placeholder="e.g. Bob, n1234567, ..."/>
            <Button Text="Submit" AutomationId="IDSubmit" Margin="20,20,20,20" Command="{Binding IDSubmitCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>