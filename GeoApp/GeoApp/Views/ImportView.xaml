<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GeoApp.ImportView"
             xmlns:local="clr-namespace:GeoApp"
             Title = "Import"
             BackgroundColor="Default">

    <ContentPage.BindingContext>
        <local:ImportViewModel x:Name="viewModel"/>
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <StackLayout>
            <Label Margin="0,20,0,0" Text="Import GeoJSON Data File"
                   TextColor="Black"
                   FontSize="Large"
                   VerticalOptions="CenterAndExpand" 
                   HorizontalOptions="CenterAndExpand" />

            <Button AutomationId="ImportButton" Margin="20,2,20,40" Text="Import File" Command="{Binding ButtonClickCommand}"/>
            
            <Label Margin="0,0,0,0" Text="Import via Paste"
                   TextColor="Black"
                   FontSize="Large"
                   VerticalOptions="Center" 
                   HorizontalOptions="CenterAndExpand" />
            <Editor HeightRequest="280" BackgroundColor="#f2f2f2" Margin="10,0,10,0" Placeholder="Paste valid GeoJSON here..." Text="{Binding TextEntry}" />
            <Button AutomationId="ImportButton" Margin="20,0,20,300" Text="Import Text" Command="{Binding TextButtonClickCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>